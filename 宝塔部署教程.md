# 短视频去水印系统 · 宝塔面板完整搭建教程

本教程指导你在 **宝塔面板（Linux）** 上从零搭建一套完整的短视频去水印系统，包含前台（单页解析）、后台管理、用户注册登录、点数付费（卡密 + 易支付）。

系统环境要求：`PHP 7.4+（建议 8.0/8.1）`、`MySQL 5.7+ / MariaDB`、`Nginx`。

---

## 一、准备宝塔环境

### 1. 安装宝塔面板
参考宝塔官网：`https://www.bt.cn/new/download.html`（按你的服务器系统选择命令安装）。安装完成后访问面板地址登录。

### 2. 安装运行环境
在宝塔面板「软件商店」中安装：

| 软件 | 版本建议 | 用途 |
|------|---------|------|
| Nginx | 1.22 以上 | Web 服务器 |
| MySQL | 5.7 / 8.0 | 数据库 |
| PHP | 8.0 / 8.1 | 运行环境（本项目源码为 PHP） |
| phpMyAdmin | 最新 | 方便管理数据库 |

> 注意：不需要安装 PHPMyAdmin 也可以，直接用宝塔「数据库」功能 + SQL 命令行导入。

### 3. 确认 PHP 扩展已开启
进入「软件商店」→ 找到已安装的 PHP → 点击「设置」→「安装扩展」，确认以下扩展已开启：

- `pdo_mysql`（必须）
- `curl`（必须，解析请求依赖）
- `fileinfo`（建议）
- `openssl`（建议，HTTPS 需要）
- `mbstring`（建议）

---

## 二、创建站点

1. 宝塔「网站」→「添加站点」。
2. 域名填写你要绑定的域名（如 `wm.example.com`），没有域名可以先填服务器 IP。
3. **PHP 版本选择 8.0 或 8.1**。
4. 点击「提交」创建站点。
5. 在站点列表中点击该站点 →「站点目录」→ 将「运行目录」设置为 `/public`（关键步骤）。

> 站点目录设置后，网站根目录指向源码包内的 `public/` 目录，`app/`、`sql/` 等核心目录不会暴露到公网。

### 伪静态（可选）
本项目使用直接 URL 访问，无需伪静态。如果你需要隐藏 `admin` 目录名等，可在「配置文件」中自行添加 Nginx rewrite 规则。

---

## 三、上传源码

1. 在本机将源码包（`wm/` 目录）压缩为 zip。
2. 宝塔「文件」→ 进入站点目录 `/www/wwwroot/你的域名/` → 删除默认文件 → 上传 zip → 解压。
3. 解压后确保目录结构为：
   ```
   /www/wwwroot/你的域名/
   ├── public/        # 网站根目录（前台 + 后台）
   ├── app/           # 核心代码
   ├── sql/           # 数据库脚本
   ```

---

## 四、初始化数据库

### 方式 A：安装向导（推荐新手）
1. 浏览器访问 `http://你的域名/install.php`。
2. 填写数据库信息（见「五、创建数据库」中获取的账号密码）和管理员账号密码。
3. 点击「开始安装」，出现「安装成功」页面即完成。
4. **立即删除 `public/install.php`**，防止被他人重装。
5. 建议站点开启 HTTPS，并将「站点地址」填写为 `https://你的域名`，保证支付回调与登录 Cookie 走安全通道。

### 方式 B：手动导入
1. 宝塔「数据库」→「添加数据库」，创建数据库（如 `wm`），并设置用户名密码。
2. 在「数据库」列表中点击「管理」（phpMyAdmin）→ 导入 `sql/install.sql`。
3. 复制 `app/config.local.example.php` 为 `app/config.local.php`，修改其中的数据库连接信息。
4. 手动创建管理员（在 phpMyAdmin 的 SQL 执行）：
   ```sql
   INSERT INTO admins(username,password,created_at) VALUES('admin','这里填PHP的password_hash值',NOW());
   ```
   > password_hash 值可先在服务器上运行 `php -r "echo password_hash('你的密码', PASSWORD_DEFAULT);"` 得到。

---

## 五、创建数据库（方式 A 需要）

1. 宝塔「数据库」→「添加数据库」。
2. 数据库名建议 `wm`，填写用户名密码并记下（用于安装向导填写）。
3. 完成后，安装向导会使用这个账号自动创建表结构。

---

## 六、访问系统

| 模块 | 地址 |
|------|------|
| 前台 | `http://你的域名/` |
| 安装向导 | `http://你的域名/install.php`（安装后删除） |
| 后台管理 | `http://你的域名/admin/index.php` |

---

## 七、后台配置

登录后台（默认在安装时设置的管理员账号），按需配置：

### 1. 站点设置
- **演示模式**：开启后任意链接返回演示结果，用于完整体验注册→充值→扣点→出结果的流程；上线时关闭。
- **每次解析消耗点数**：默认 1。
- **注册赠送点数**：默认 3。
- **1 元兑换点数**：默认 10（即 10 元=100 点）。

### 2. 卡密充值（用户可用）
「卡密管理」→ 设置数量与每张点数 →「批量生成」，把生成的卡密发给用户即可。
用户在「充值中心 → 卡密充值」输入卡密，点数自动到账。

### 3. 易支付（在线支付）
在「站点设置 → 易支付配置」填写：
- 支付通道开关：开启
- 可用支付方式：`alipay,wxpay`
- 易支付 API 地址：如 `https://pay.example.com`
- 商户 ID (pid)、商户 KEY

然后在 **易支付平台后台** 将「异步通知地址」配置为：
```
http://你的域名/api.php?action=pay_notify
```
（支持 HTTPS 时用 `https://`，回调地址需与站点一致）

### 4. 手动充值
用户管理 → 点击用户「加点」即可手动到账（适合收款后手动处理）。

---

## 八、多平台解析说明

系统支持两种解析方式，可在后台「站点设置」切换：

### 方式一：自定义解析接口（推荐）
如果你购买了第三方聚合解析接口（自动识别抖音/快手/小红书等平台），在后台「自定义解析接口」填写：
- 接口开关：开启
- 接口完整请求地址：直接粘贴整段请求地址，末尾以 `url=` 结尾，例如：
  `https://syapi.chuangye.site/home/api?type=dsp&uid=5818526&key=xxxx&url=`

系统会自动把分享链接拼接到 `url=` 后面，并解析返回的 JSON。返回字段支持：`title`、`desc`、`cover`、`playAddr`（视频直链）、`pics`（图集）、`type`、`platform`、`author`（作者）、`music`（背景音乐）、`video_backup`（备用视频）、`source`；返回码 `code=0001` 表示成功。

### 方式二：内置解析器（框架扩展）
解析框架位于 `app/parsers/`，采用接口化设计，新增平台步骤：

1. 新建 `app/parsers/XxxParser.php`，继承 `BaseParser` 实现 `key()`、`supports()`、`parse()`。
2. 在 `app/ParserFactory.php` 的 `$parsers` 数组注册类名。

已内置：`DouyinParser`（抖音）、`KuaishouParser`（快手）、`ApiParser`（自定义接口）。未配置远程接口时会自动回退到内置抖音 / 快手解析器。

> 抖音/快手平台接口会不定期调整。若内置解析报「接口未返回数据」，通常需要更新对应解析器里的接口地址和字段解析，此框架已预留扩展点，字段解析集中在单一文件便于维护。

---

## 九、常见问题

**Q1：安装向导报「服务器 PHP 缺少 pdo_mysql 扩展」**
A：宝塔 PHP 设置 → 安装扩展 → 安装 `pdo_mysql`，然后重载 PHP。

**Q2：解析提示「点数不足」**
A：用户需先充值。可在后台给用户加点、发卡密，或开通易支付。

**Q3：易支付支付成功但点数没到账**
A：检查三点：① 站点回调地址是否配置正确且公网可访问；② 商户 KEY 是否一致；③ 服务器防火墙是否放行回调。可在 `public/api.php` 的 `pay_notify` 里临时加日志排查。

**Q4：抖音解析失败**
A：平台接口更新导致的常见现象，更新 `app/parsers/DouyinParser.php` 中的接口地址与字段映射即可；上线前建议先在演示模式验证完整流程。

**Q5：如何防刷**
A：系统已内置同一用户 1 秒一次的解析频率限制；如需更严格限制，可在 Nginx 或宝塔「防护」中加 IP 限速规则。

---

## 十、目录结构速览

```
wm/
├── public/                    # 网站根目录（宝塔运行目录指向这里）
│   ├── index.php              # 前台单页
│   ├── api.php                # 前台 API
│   ├── install.php            # 安装向导（安装后删除）
│   ├── admin/
│   │   ├── index.php          # 后台单页
│   │   └── api.php            # 后台 API
│   └── assets/css|js/         # 静态资源
├── app/
│   ├── config.php             # 全局配置（读取 config.local.php）
│   ├── config.local.php       # 本地配置（安装生成或手动创建）
│   ├── config.local.example.php
│   ├── db.php                 # PDO 数据库封装
│   ├── functions.php          # 通用函数
│   ├── auth.php               # 会话认证
│   ├── ParserFactory.php      # 解析器工厂
│   ├── parsers/               # 各平台解析器（多平台框架）
│   └── payment/Epay.php       # 易支付对接
└── sql/install.sql            # 数据库初始化脚本
```
