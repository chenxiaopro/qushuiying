# 短视频去水印系统 · 宝塔面板完整搭建教程

本教程指导你在 **宝塔面板（Linux）** 上从零搭建一套完整的短视频去水印系统，包含前台单页解析、用户中心、后台管理、点数付费（卡密 + 易支付 + 支付宝当面付）、公众号签到。

全部后台字段与用户操作说明见项目文档 `.monkeycode/docs/使用与配置.md`。

系统环境要求：`PHP 7.4+（建议 8.0/8.1）`、`MySQL 5.7+ / MariaDB`、`Nginx`。

---

## 一、准备宝塔环境

### 1. 安装宝塔面板
参考宝塔官网：`https://www.bt.cn/new/download.html`（按你的服务器系统选择命令安装）。安装完成后访问面板地址登录。

### 2. 安装运行环境
在宝塔面板「软件商店」中安装：

| 软件 | 版本建议 | 用途 |
|------|---------|------|
| Nginx | 1.22 以上 | Web 服务器 |
| MySQL | 5.7 / 8.0 | 数据库 |
| PHP | 8.0 / 8.1 | 运行环境（本项目源码为 PHP） |
| phpMyAdmin | 最新 | 方便管理数据库 |

> 注意：不需要安装 PHPMyAdmin 也可以，直接用宝塔「数据库」功能 + SQL 命令行导入。

### 3. 确认 PHP 扩展已开启
进入「软件商店」→ 找到已安装的 PHP → 点击「设置」→「安装扩展」，确认以下扩展已开启：

- `pdo_mysql`（必须）
- `curl`（必须，解析请求依赖）
- `fileinfo`（建议）
- `openssl`（建议，HTTPS 需要）
- `mbstring`（建议）

---

## 二、创建站点

1. 宝塔「网站」→「添加站点」。
2. 域名填写你要绑定的域名（如 `wm.example.com`），没有域名可以先填服务器 IP。
3. **PHP 版本选择 8.0 或 8.1**。
4. 点击「提交」创建站点。
5. 在站点列表中点击该站点 →「站点目录」→ 将「运行目录」设置为 `/public`（关键步骤）。

> 站点目录设置后，网站根目录指向源码包内的 `public/` 目录，`app/`、`sql/` 等核心目录不会暴露到公网。

### 伪静态（可选）
本项目使用直接 URL 访问，无需伪静态。如果你需要隐藏 `admin` 目录名等，可在「配置文件」中自行添加 Nginx rewrite 规则。

---

## 三、上传源码

1. 在本机将源码包（`wm/` 目录）压缩为 zip。
2. 宝塔「文件」→ 进入站点目录 `/www/wwwroot/你的域名/` → 删除默认文件 → 上传 zip → 解压。
3. 解压后确保目录结构为：
   ```
   /www/wwwroot/你的域名/
   ├── public/        # 网站根目录（前台 + 后台）
   ├── app/           # 核心代码
   ├── sql/           # 数据库脚本
   ```

---

## 四、初始化数据库

### 方式 A：安装向导（推荐新手）
1. 浏览器访问 `http://你的域名/install.php`。
2. 填写数据库信息（见「五、创建数据库」中获取的账号密码）和管理员账号密码。
3. 点击「开始安装」，出现「安装成功」页面即完成。
4. **立即删除 `public/install.php`**，防止被他人重装。
5. 建议站点开启 HTTPS，并将「站点地址」填写为 `https://你的域名`，保证支付回调与登录 Cookie 走安全通道。

### 方式 B：手动导入
1. 宝塔「数据库」→「添加数据库」，创建数据库（如 `wm`），并设置用户名密码。
2. 在「数据库」列表中点击「管理」（phpMyAdmin）→ 导入 `sql/install.sql`。
3. 复制 `app/config.local.example.php` 为 `app/config.local.php`，修改其中的数据库连接信息。
4. 手动创建管理员（在 phpMyAdmin 的 SQL 执行）：
   ```sql
   INSERT INTO admins(username,password,created_at) VALUES('admin','这里填PHP的password_hash值',NOW());
   ```
   > password_hash 值可先在服务器上运行 `php -r "echo password_hash('你的密码', PASSWORD_DEFAULT);"` 得到。

---

## 五、创建数据库（方式 A 需要）

1. 宝塔「数据库」→「添加数据库」。
2. 数据库名建议 `wm`，填写用户名密码并记下（用于安装向导填写）。
3. 完成后，安装向导会使用这个账号自动创建表结构。

---

## 六、访问系统

| 模块 | 地址 |
|------|------|
| 前台 | `http://你的域名/` |
| 安装向导 | `http://你的域名/install.php`（安装后删除） |
| 后台管理 | `http://你的域名/admin/index.php` |

---

## 七、后台配置

登录后台（安装时设置的管理员账号），按需配置。更细的字段说明见 `.monkeycode/docs/使用与配置.md`。

建议先在 `app/config.local.php` 把 `site_url` 写成 `https://你的域名`，支付回调和公众号 URL 才稳定。

### 1. 站点设置 → 基础设置
- **站点名称 / 描述 / 公告**：页标题、首页文案、公告弹窗。
- **每次解析消耗点数**：默认 1，解析成功后才扣。
- **注册赠送点数**：默认 3。
- **1 元兑换点数**：默认 10（10 元 = 100 点）。
- **当前版本号**：只读，由「历史版本」新增记录时自动生成。

代码里没有「演示模式」开关。

### 2. 解析类型 + 接口管理（解析能不能出结果，主要看这里）
1. 「解析类型」确认前台需要的选项（默认有 `dyzy` / `dsp` / `douyinyh` / `live`）。
2. 「接口管理」→「新增接口」：
   - 标识 slug：平台如 `douyin`，或 `*` 通用
   - 解析类型：与上一步的 key 对应，可留空
   - 接口地址：完整 URL，query 以 `url=` 结尾，例如：
     `https://syapi.chuangye.site/home/api?type=dsp&uid=XXX&key=XXX&url=`
   - 字段映射建议：`resp_title=title`，`resp_video=playAddr`，`resp_img=cover`

未配置远程接口时，仅回退内置抖音 / 快手解析器。

### 3. 卡密充值
「卡密管理」→ 数量 1–500、每张点数 →「批量生成」。用户在前台充值弹窗「卡密充值」输入。超过 7 天未使用的卡会自动清理。

### 4. 易支付
「站点设置 → 易支付配置」：
- 支付通道开关：开启
- 可用支付方式：`alipay,wxpay`（还可 `qqpay,bank,jdpay`）
- API 地址、商户 ID、**平台公钥**、**商户私钥**（RSA，不是旧版商户 KEY）
- 保存后点「密钥自检」

易支付商户后台：
- 异步通知：`https://你的域名/pay_notify.php`
- 同步跳转：`https://你的域名/pay_return.php`

### 5. 支付宝当面付
「站点设置 → 支付宝当面付」：开关、App ID、应用私钥（RSA2）、支付宝公钥（验签用）。通知地址由系统在下单时自动带上：`https://你的域名/alipay_notify.php`。

### 6. 公众号弹窗引导
独立开关。填名称、二维码图 URL、文案后，前台会弹关注窗；用户关闭后 7 天内不再显示。

### 7. 公众号签到
与弹窗是两套配置。
1. 打开签到开关，填每日点数、AppID、AppSecret、Token 并保存
2. 公众平台「基本配置」启用服务器，URL 填后台展示的 `https://你的域名/wxmp.php`，明文模式，IP 白名单填服务器公网 IP
3. 后台「检测配置」通过后，「创建自定义菜单」
4. 用户登录网站，打开用户中心**概览**复制 6 位绑定码，发给公众号；之后点菜单「每日签到」

开关打开后前台就会显示绑定码；创建菜单仍须 AppID/Secret/Token 齐全。

### 8. 分享卡片与 Bark
- 分享标题/描述/封面：微信、QQ 识别链接时的卡片
- Bark：管理员收「新用户注册」「在线充值成功」推送（卡密不推）

### 9. 手动充值
用户管理 →「加点」。会计入累计充值。还可改邮箱、解绑微信、重置密码、禁用账号。

---

## 八、多平台解析说明

解析入口在后台「解析类型」和「接口管理」，不在站点设置里切换总开关。

### 方式一：远程接口（推荐）
「接口管理」新增，地址末尾 `url=`，系统把分享链接拼上去。同一平台可多条，取排序值最小的启用记录。slug=`*` 表示通用。

返回码示例 `code=0001`。常用字段：`title`、`cover`、`playAddr`、`pics`、`type`、`platform`、`author`、`music`、`video_backup`。`type`：1 视频 / 2 图集 / 3 实况。

### 方式二：内置解析器
`app/parsers/` 下继承 `BaseParser`，在 `ParserFactory.php` 远程接口都未命中后调用。已内置抖音、快手。平台改接口后优先换远程地址。

---

## 九、常见问题

**Q1：安装向导报「服务器 PHP 缺少 pdo_mysql 扩展」**
A：宝塔 PHP 设置 → 安装扩展 → 安装 `pdo_mysql`，然后重载 PHP。

**Q2：解析提示「点数不足」**
A：用户需先充值。可在后台给用户加点、发卡密，或开通易支付。

**Q3：易支付支付成功但点数没到账**
A：检查：① 异步通知是否为 `https://你的域名/pay_notify.php` 且公网可访问；② 平台公钥 / 商户私钥是否与易支付后台一致（先做「密钥自检」）；③ `site_url` 是否 HTTPS。日志看 `public/pay_notify.php` 与 Web 服务器 access log。

**Q4：抖音解析失败**
A：优先在「接口管理」更换可用远程接口。内置解析器随平台改版可能失效。

**Q5：如何防刷**
A：同一用户解析间隔 2 秒；登录/注册有 IP 次数限制。可再在 Nginx 或宝塔「防护」加 IP 限速。

**Q6：前台没有微信绑定码**
A：后台「公众号签到」开关必须打开。打开后用户中心「概览」会自动显示 6 位码。创建菜单才需要 AppID/Secret/Token。

**Q7：公众号菜单创建失败**
A：先「检测配置」。`40164` 把服务器公网 IP 加入公众平台白名单；`48001` 当前号类型不支持自定义菜单。

---

## 十、目录结构速览

```
wm/
├── public/                    # 网站根目录（宝塔运行目录指向这里）
│   ├── index.php              # 前台单页
│   ├── api.php                # 前台 API
│   ├── install.php            # 安装向导（安装后删除）
│   ├── download.php           # 登录后下载/预览代理
│   ├── wxmp.php               # 公众号服务器 URL
│   ├── pay_notify.php         # 易支付异步通知
│   ├── pay_return.php         # 易支付同步跳转
│   ├── alipay_notify.php      # 当面付异步通知
│   ├── admin/
│   │   ├── index.php          # 后台单页
│   │   └── api.php            # 后台 API
│   └── assets/css|js/         # 静态资源
├── app/
│   ├── config.php             # 全局配置（读取 config.local.php）
│   ├── config.local.php       # 本地配置（安装生成或手动创建）
│   ├── config.local.example.php
│   ├── db.php                 # PDO 数据库封装
│   ├── functions.php          # 通用函数
│   ├── auth.php               # 会话认证
│   ├── ParserFactory.php      # 解析器工厂
│   ├── WechatMp.php           # 公众号绑定与签到
│   ├── NotifyBark.php         # Bark 通知
│   ├── parsers/               # 各平台解析器
│   └── payment/               # 易支付、支付宝当面付
└── sql/install.sql            # 数据库初始化脚本
```
